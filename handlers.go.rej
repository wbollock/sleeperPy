--- handlers.go
+++ handlers.go
@@ -1197,6 +1197,14 @@
 
 				ownerName := "You"
 				originalName := ""
+				
+				// Sanity check: Verify this pick is actually owned by the user
+				// If the pick was traded away, it shouldn't appear here
+				if ownerRosterID != int(userRosterID) {
+					debugLog("[DEBUG] WARNING: Pick extraction mismatch - skipping pick")
+					debugLog("[DEBUG]   Expected owner: %.0f, Actual owner: %d", userRosterID, ownerRosterID)
+					continue
+				}
 
 				// If this pick was traded (original owner != current owner)
 				if originalRosterID != int(userRosterID) {
